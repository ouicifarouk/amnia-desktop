# amnia-desktop

This is a Tauri desktop app with three layers:

- Rust core (`src-tauri/`)
- JS/Tailwind UI (`src/`)
- Python AI sidecar (`python_sidecar/`)

The initial scaffold lives in PR #1 against this branch.
